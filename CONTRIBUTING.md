# Contributing

Use a vertical slice: add a failing behavioral test, capture it in `docs/development-log.md`, implement the smallest change, then run tests, evals, lint, and type checks. Do not weaken default-deny policy without a reviewed threat-model update.
